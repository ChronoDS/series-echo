import React from "react";

export const Header = () => {

    const handleDelete = (funcParameter1, funcParameter2) => {
        // func body
    };

    return (
        <div>
            Header Series Echo B-Ach:
        </div>
    )
};
